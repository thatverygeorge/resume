---
interface Props {
  name: string;
  position: string;
  techStack: string[];
}

const { name, position } = Astro.props;
---

<section aria-labelledby="resume-heading">
  <h1 id="resume-heading">
    {name}
    <br />
    {position}
  </h1>
</section>
